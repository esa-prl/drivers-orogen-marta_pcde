# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(pcdeTaskLib)
ADD_LIBRARY(${PCDE_TASKLIB_NAME} SHARED 
    ${PCDE_TASKLIB_SOURCES})
add_dependencies(${PCDE_TASKLIB_NAME}
    regen-typekit)



TARGET_LINK_LIBRARIES(${PCDE_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${PCDE_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${PCDE_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${PCDE_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${PCDE_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${PCDE_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${PCDE_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${PCDE_TASKLIB_HEADERS}
    DESTINATION include/orocos/pcde)

